/*
Problem: Create a function that accepts any number of keyword arguments
and prints them in the format key: value.
*/
# **kwargs is used to handle key,value pairs. **kwargs means it can receive multiple key, value arguments.
def print_kwargs(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_kwargs(name="Alice", power="lazer")
print_kwargs(name="Alice", power="lazer", enemy="Bob")
